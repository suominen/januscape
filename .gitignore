# Claude Code — local/personal settings only (shared settings.json is tracked)
.claude/settings.local.json

# direnv cache
.direnv/

# Nix build outputs
result
result-*

# Hugo build artifacts
.hugo_build.lock
site/_vendor/
site/public/
site/resources/

# Editor / OS
*~
.DS_Store

# Scratch files the headless agent's restricted permission set (no rm/mv)
# could not remove after an accidental curl -o write; see commit message.
/.dt.json.tmp
/scratch_probe.txt
